# Business plan — example product

## Why now {#why-now}

The deadline is fixed and published. [S1]

Members expect to buy rather than build. [S9]

`[S9]` is the defect: the log assigns S1 and S2 and stops there, so the second
citation carries the appearance of provenance and none of the substance.

## How a source is cited

The block below is the template this corpus writes its own citations to, and it
is fenced because it is an example rather than an assertion. A scan that read
fenced lines would report the S404 in it as a citation with no row, and a check
that fires on a document for documenting its own format is one somebody
switches off.

```
Some claim the plan makes. [S404]
```
