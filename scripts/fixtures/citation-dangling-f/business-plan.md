# Business plan — example product

The document a reader holds. It cites the founder brief in prose, which is the
address `--used-in` deliberately does not open: a note names a document and an
anchor, and a code names a row in an index.

## Why now {#why-now}

Producers must re-label before the deadline. [F1]

The regulated tier is the second half of the same clock. [F7]

`[F7]` is the defect this fixture exists for. The brief carries F1 and F2 and
nothing else, so the second citation resolves to nothing — and it renders
exactly like the first one, which is why no other check in this corpus can see
it. The reader who follows it is the one person who cannot check it.
