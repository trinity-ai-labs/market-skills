---
id: CLAIM-AC88HH08
type: claim
title: "Example Ledger is a direct competitor and leaves the multi-entity job uncovered"
status: current
confidence: H
confidence_own: H
subject: "price-anchor"
created: "2026-01-20"
actor: "ledger-example-com"
stale_after: "2099-12-31"
rests_on:
  - SOURCE-AC11AA01
---

Violates: actor-type-not-shippable

THE PRIVACY SEAM, AND THE HALF NO GREP OVER THIS REPO CAN SEE. This note carries a
bucket and a wedge line — both judgements about a subject against the jobs one
dossier decomposed — authored as an actor note directly inside a vault. The
repo-side check is a grep for a `CLAIM-*` file under the corpus directory, and with
only that half this is the cheapest way past the seam: no filename in the corpus is
involved at all.

A record is `source` and `fact` notes only, and that is not an arbitrary
restriction. Every output a dossier recomputes per engagement is a judgement, so
every one of them would have to be a `claim` — which is exactly what makes the
seam checkable rather than trusted.

`stale_after` is far out so the note is reported for its type alone: a stale-claim
failure beside it would keep the count right with `actor-type-not-shippable`
deleted. The three fields a `fact` owes are deliberately absent, because they are
owed on a `fact` and this note is the case where the type itself is the failure.
