# Business plan — example product

The document both superseded claims were cited into. It exists, and both anchors
resolve, so `--used-in` passes over this vault — which is what makes it the
fixture for a gate that fails on the sweep alone.

## Why now

The section `CLAIM-UR1CC003` named. It still asserts the version that note was
replaced for, and nothing mechanical can tell: the prose cites `[F1]`, not the
note ID.

## Risks

The section `CLAIM-UR1EE005` named, in the same state and for the same reason.
