# Business plan — example product

The document every superseded note in this vault was cited into. Both anchors
resolve, so `--used-in` passes here — which is what makes the sweep the only
part of the gate that can see what is wrong.

## Why now

The section `ASSUMPTION-HE1BB002` named. Its supersession is written from both
ends, so the sweep names this section and nothing else is owed.

## Pricing

The section `ASSUMPTION-HE1CC003` named. That note records its own replacement
in `superseded_by` and the named successor never wrote the other half, so before
this check the sweep reported it as replaced by nothing at all — and a reader
sent to decide what replaced it never found the successor the record already
names.
