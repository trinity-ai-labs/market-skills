# Business plan — example product

The passing side. Every code this document cites resolves, and the two index
files each carry a mention of a retired code that the mode never reads because
it never opens them.

## Why now {#why-now}

Producers must re-label before the deadline. [F1] The deadline itself is fixed
and published. [S1]

Members expect to buy rather than build. [S2]
