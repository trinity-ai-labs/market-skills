# Sources — example product

The global log, carrying the one thing that makes excluding it from the scan
load-bearing rather than an optimisation: a record of a code that was withdrawn
and deliberately left unused.

[S3] was withdrawn after the publisher retracted the figure behind it, and the
number is left unassigned on purpose — a reader who finds [S3] in an older
draft can tell that the code was retired rather than mistyped. A scan that read
this log's own prose as citations would report both of those mentions as codes
with no row and fail a corpus doing exactly the right thing.

| Code | Source | URL | Used in |
|---|---|---|---|
| [S1] | Regulator, deadline notice | https://example.org/deadline | business-plan.md |
| [S2] | Trade body, member survey | https://example.org/survey | business-plan.md |
