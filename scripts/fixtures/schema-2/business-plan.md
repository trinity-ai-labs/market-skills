# Business plan — example product

Enough of a document for the notes in this vault to cite. The lint ignores
non-note files at the vault root; `--used-in` opens them. It exists so
`--release-gate` stays clean at schemaVersion 2 — a fixture whose gate goes red
for a missing document asserts nothing about the version.

## Why now {#why-now}

The section `CLAIM-SV2HH008` reached before it was superseded. Producers must
re-label before the deadline, and the ones without an in-house regulatory
function buy tooling rather than build it. [F1]

## Milestones & roadmap {#roadmap}

Three items, sequenced. Two consume founder hours and run one after the other;
the third is gated on an external certification clock and runs during the wait,
which is what Rule 4 buys.

Every item cell below is a milestone `title` copied verbatim, because the table
is rendered off the notes rather than written beside them. This is the passing
side of `--roadmap-table`, and it is the assertion with teeth: a check keyed on
anything looser than the exact string would pass here whatever it did, and the
two vaults next door are what catch each direction of the drift.

| Item | Assumption moved | Resource | Sequence |
|---|---|---|---|
| Unassisted setup for the core job ships | setup effort per account (A-1) | founder-hours | 1 |
| Certification for the regulated tier is filed and granted | regulated-tier availability (A-2) | external-certification-clock | 1 |
| Self-serve renewal lands for the accounts the deadline brought in | renewal handling (A-3) | founder-hours | 2 |

| Order | 12-mo cumulative | Δ vs chosen |
|---|---|---|
| Setup → certification → renewal | — | — |
| Certification → setup → renewal | — | within noise |

The second table is the permutation comparison of Rule 3, and it is here on
purpose: its first column is an ORDER rather than an item, so a check reading
every table in this section would report both rows as items that escaped the
ledger. Only the first table is read.

## Financial summary

Deliberately empty — nothing cites it.
