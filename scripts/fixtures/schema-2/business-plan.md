# Business plan — example product

The citation target `CLAIM-SV2DD004` names. It exists so `--used-in` resolves
over this vault and `--release-gate` stays clean at schemaVersion 2 — a fixture
whose gate goes red for a missing document asserts nothing about the version.

## Why now

Producers must re-label before the deadline, and the ones without an in-house
regulatory function buy tooling rather than build it. [F1]
