# One pager — example product

`CLAIM-AS23SD44` names this document in `used_in` with no `#` after it, so the
entry is a file-existence check and nothing more. A note can legitimately cite a
document whose sections it does not name, and demanding an anchor would make the
mode fail on the shape the schema allows.

Producers must re-label before the deadline. [F1]
