# Red team — example product

A panel ran and its objections are here. What is not here is any record of which
lenses were sent, which at `schemaVersion` 2 is the failure: a lens that returned
findings and wrote no row is indistinguishable from one that had no objections,
and there is nothing to compare the table against.

It is also the case the reverse direction cannot cover on its own. With no
roster there are no entries to go unmatched, so the only way this document fails
is a check that asks for the roster itself — otherwise the cheapest route past
the whole mode is to delete the section.

## Objections

| # | Lens | Objection | Severity | Disposition |
|---|---|---|---|---|
| R1-O1 | Capital skeptic | The addressable set is the producers who re-label, not every producer in the category. | High | fixed |
| R1-O2 | Operator | The roadmap assumes two engineers from month one and the plan funds one. | Medium | moved to Risks as R1-O2 |
