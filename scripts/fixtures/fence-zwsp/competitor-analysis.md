# Competitor Analysis — example product

The other half of the zero-width-space fixture. The `## Monitoring plan` heading
here is clean, so the section IS entered — what carries the zero-width space is
the fence inside it, which is the comparison `bin/vault-lint.ps1:2249` names and
three sibling copies of the same scan did not apply.

## Monitoring plan

| Axis | Instrument | Cadence | Decision it would change |
|---|---|---|---|
| Whether the incumbent's​ packaging is moving toward a bundle | Their pricing page diffed against the stored copy | Monthly | Whether the price wedge survives |
| Whether the deadline slips | | Quarterly | Whether the first milestone moves with it |

The first axis carries a zero-width space inside its name and answers all three
columns, so it stays silent: an axis is not matched against anything outside this
table, and the fold is what keeps a capitalisation difference from firing.

The second names no instrument, which is the one failure this document owes.

The fence below opens with three backticks followed by a zero-width space and
closes the same way. Its row would read as an axis naming no instrument, no
cadence and no decision if the fence scan folded that character away — so an
extra `monitoring-axis-incomplete` row here is the divergence, reported rather
than invisible.

```​
| Axis | Instrument | Cadence | Decision it would change |
| <what to watch> | <how it is read> | <how often> | <what it changes> |
```​

## Coverage

Two axes named, one of them complete.
