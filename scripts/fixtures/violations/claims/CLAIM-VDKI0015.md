---
id: CLAIM-VDKI0015
type: claim
title: "The ceiling holds at the trial flow one channel supports"
status: current
confidence: M
confidence_own: M
created: "2026-07-20"
subject: "steady-state-ceiling"
stale_after: "2099-12-31"
binding_driver: "trial flow"
driver_kind: strucural
evidence_n: "1"
evidence_counterparties: "1"
rests_on:
  - FACT-DANG0002
---

Violates: driver-kind-unknown

A fourth word, and a typo rather than an invention — which is the point. An
unrecognised `driver_kind` takes the structural path by default, so a typo and a
deliberate classification are indistinguishable to every consumer and the plan
reports a decision the founder made as a category floor.

All four fields a ceiling owes once it carries one are here, so
`verdict-fields-incomplete` stays silent, and `conditional_on` is not owed
because `strucural` is not one of the two policy values. The closure under it
reaches one source note from one counterparty, which is thin — and the two counts
say so, so `verdict-thin-evidence` stays silent. That is the surfaced side of the
rule, asserted here so a check that ignored a stated count would turn this fixture
red rather than passing a census untouched.
